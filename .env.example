# Environment Configuration Example
# Copy this file to `.env` and modify the values if necessary.

# Path to the Gemini CLI command (if it's in your system PATH, 'gemini' is sufficient)
GEMINI_CLI_COMMAND=gemini

# Default Edge-TTS Settings
DEFAULT_TTS_VOICE=en-US-ChristopherNeural
DEFAULT_TTS_RATE=-10%
